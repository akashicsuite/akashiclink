import styled from '@emotion/styled';

/**
 * Small dark purple text
 * TODO: Define this as an h3 style in the css
 */
export const ContentText = styled.span({
  fontWeight: 700,
  fontSize: '18px',
  lineHeight: '24px',
  color: 'var(--ion-color-primary-10)',
});
